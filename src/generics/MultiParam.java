package generics;

/**
 * Created by f.taleb on 7/1/2017.
 */
 /* The fifth example has 2 type parameters (see "multiple type parameters" below),
          choosing the second parameter as the type parameter passed to the superclass.*/
public class MultiParam<T,E> extends AbstractParam<E> {
}
