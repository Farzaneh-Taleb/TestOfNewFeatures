package generics;

/**
 * Created by f.taleb on 7/1/2017.
 */

/*  The fourth example doesn't use generics at all, so it's similar to if the class had an Object parameter.
        The compiler will warn about AbstractParam being a raw type, but it will compile the ObjectParam class.
*/

public class ObjectParam extends  AbstractParam {
}
